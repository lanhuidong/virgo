<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexusy.virgo.data.mapper.TodoMapper">
    <insert id="insertTodo">
        insert into todo (created, content, remind_at, finished, repeat_type, user_id) values (#{created}, #{content},
        #{remindAt}, #{finished}, #{repeatType}, #{userId})
    </insert>

    <select id="selectTodo" resultType="com.nexusy.virgo.data.model.Todo">
        select * from todo where id = #{id}
    </select>

</mapper>